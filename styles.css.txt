:root {
  --bg: #0b0f14;
  --card: #111826;
  --alt: #0f1520;
  --text: #e9eef6;
  --muted: #a7b2c3;
  --accent: #7aa2ff;
  --border: rgba(255, 255, 255, 0.08);
  --shadow: 0 10px 30px rgba(0, 0, 0, 0.35);
  --radius: 18px;
}

* { box-sizing: border-box; }
html, body { margin: 0; padding: 0; }
body {
  font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial;
  background: radial-gradient(1200px 600px at 20% 10%, rgba(122,162,255,0.18), transparent 60%),
              radial-gradient(900px 500px at 85% 20%, rgba(122,162,255,0.12), transparent 55%),
              var(--bg);
  color: var(--text);
  line-height: 1.55;
}

a { color: inherit; text-decoration: none; }
code { background: rgba(255,255,255,0.06); padding: 0.15rem 0.35rem; border-radius: 8px; }

.container { width: min(1080px, 92%); margin: 0 auto; }

.header {
  position: sticky;
  top: 0;
  z-index: 20;
  backdrop-filter: blur(10px);
  background: rgba(11, 15, 20, 0.7);
  border-bottom: 1px solid var(--border);
}
.header__inner {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0.9rem 0;
}

.brand { display: inline-flex; align-items: center; gap: 0.55rem; }
.brand__dot {
  width: 12px; height: 12px; border-radius: 999px;
  background: var(--accent);
  box-shadow: 0 0 0 4px rgba(122,162,255,0.18);
}
.brand__text { font-weight: 700; letter-spacing: 0.2px; }

.nav { display: flex; align-items: center; gap: 0.8rem; }
.nav__toggle {
  display: none;
  background: transparent;
  border: 1px solid var(--border);
  color: var(--text);
  padding: 0.45rem 0.65rem;
  border-radius: 12px;
  cursor: pointer;
}
.nav__links { display: flex; gap: 1rem; }
.nav__links a {
  color: var(--muted);
  padding: 0.35rem 0.6rem;
  border-radius: 12px;
}
.nav__links a:hover { color: var(--text); background: rgba(255,255,255,0.05); }

.hero { padding: 3.2rem 0 2.2rem; }
.hero__grid {
  display: grid;
  grid-template-columns: 1.3fr 0.7fr;
  gap: 1.2rem;
  align-items: start;
}
.hero__content h1 {
  font-size: clamp(2rem, 3.2vw, 3rem);
  line-height: 1.1;
  margin: 0 0 0.8rem;
}
.lead { color: var(--muted); font-size: 1.05rem; margin: 0 0 1.2rem; }

.hero__cta { display: flex; gap: 0.8rem; margin-bottom: 1.1rem; flex-wrap: wrap; }
.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  background: var(--accent);
  color: #08101f;
  font-weight: 700;
  border: none;
  border-radius: 14px;
  padding: 0.75rem 1rem;
  cursor: pointer;
  box-shadow: 0 10px 25px rgba(122,162,255,0.15);
}
.btn:hover { filter: brightness(1.05); }
.btn--ghost {
  background: transparent;
  color: var(--text);
  border: 1px solid var(--border);
  box-shadow: none;
}
.btn--small { padding: 0.55rem 0.8rem; border-radius: 12px; font-weight: 700; }

.hero__meta { display: flex; gap: 0.6rem; flex-wrap: wrap; }
.chip {
  border: 1px solid var(--border);
  background: rgba(255,255,255,0.04);
  color: var(--muted);
  padding: 0.35rem 0.6rem;
  border-radius: 999px;
  font-size: 0.92rem;
}

.section { padding: 2.2rem 0; }
.section--alt { background: linear-gradient(180deg, transparent, rgba(255,255,255,0.02), transparent); }
.section__head { margin-bottom: 1rem; }
.section__head h2 { margin: 0; font-size: 1.7rem; }
.muted { color: var(--muted); }
.small { font-size: 0.92rem; }

.grid { display: grid; gap: 1rem; }
.grid--2 { grid-template-columns: repeat(2, 1fr); }
.grid--3 { grid-template-columns: repeat(3, 1fr); }

.card {
  background: rgba(17, 24, 38, 0.85);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: 1.1rem 1.05rem;
  box-shadow: var(--shadow);
}

.divider { height: 1px; background: var(--border); margin: 1rem 0; }

.stats { display: grid; grid-template-columns: repeat(3, 1fr); gap: 0.8rem; margin-top: 0.7rem; }
.stat__num { font-size: 1.6rem; font-weight: 800; }
.stat__label { color: var(--muted); font-size: 0.9rem; }

.progress { margin-top: 0.9rem; }
.progress__bar {
  width: 100%;
  height: 12px;
  background: rgba(255,255,255,0.06);
  border-radius: 999px;
  overflow: hidden;
  border: 1px solid var(--border);
}
.progress__fill {
  height: 100%;
  background: var(--accent);
  width: 0%;
}
.progress__meta {
  display: flex; justify-content: space-between; align-items: center;
  margin-top: 0.55rem;
}

.link { color: var(--accent); cursor: pointer; background: none; border: none; padding: 0; font: inherit; }
.link:hover { text-decoration: underline; }

.list { margin: 0.8rem 0 0; padding-left: 1.2rem; color: var(--muted); }
.timeline { list-style: none; padding: 0; margin: 0.8rem 0 0; }
.timeline li { display: flex; align-items: center; gap: 0.6rem; color: var(--muted); padding: 0.35rem 0; }
.dot { width: 10px; height: 10px; border-radius: 999px; background: rgba(122,162,255,0.8); box-shadow: 0 0 0 4px rgba(122,162,255,0.12); }

.project__top { display: flex; justify-content: space-between; align-items: center; margin-bottom: 0.5rem; }
.badge {
  display: inline-flex;
  align-items: center;
  border: 1px solid var(--border);
  background: rgba(255,255,255,0.05);
  padding: 0.25rem 0.55rem;
  border-radius: 999px;
  font-size: 0.85rem;
  color: var(--text);
}
.project__links { display: flex; gap: 0.8rem; margin-top: 0.8rem; }

.feed { list-style: none; margin: 0; padding: 0; display: grid; gap: 0.7rem; }
.feed__title { display: block; font-weight: 700; }
.feed__meta { display: block; font-size: 0.92rem; }

.form { display: grid; gap: 0.75rem; margin-top: 0.6rem; }
label { display: grid; gap: 0.4rem; color: var(--muted); font-size: 0.95rem; }
input, textarea {
  width: 100%;
  border-radius: 14px;
  border: 1px solid var(--border);
  background: rgba(255,255,255,0.04);
  color: var(--text);
  padding: 0.7rem 0.75rem;
  outline: none;
}
input:focus, textarea:focus { border-color: rgba(122,162,255,0.55); }

.footer { padding: 1.6rem 0 2.2rem; }
.footer__inner { display: flex; justify-content: space-between; align-items: center; gap: 1rem; }

@media (max-width: 900px) {
  .hero__grid { grid-template-columns: 1fr; }
  .grid--3 { grid-template-columns: 1fr; }
  .grid--2 { grid-template-columns: 1fr; }
  .nav__toggle { display: inline-flex; }
  .nav__links {
    display: none;
    position: absolute;
    right: 4%;
    top: 60px;
    background: rgba(17, 24, 38, 0.98);
    border: 1px solid var(--border);
    border-radius: 14px;
    padding: 0.6rem;
    flex-direction: column;
    min-width: 180px;
  }
  .nav__links a { display: block; }
  .nav__links.is-open { display: flex; }
}
